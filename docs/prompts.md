# 提出物③ ── 設計・実装に用いた主要プロンプト全文

アプリ名：**ヨルログ** ／ 単一HTML：`index.html`
LLM API：Anthropic Messages API（既定 `claude-opus-5`、OpenAI・Gemini にも切替可）
呼び出しは全て **構造化出力（JSON Schema）** を指定し、表示崩れを起こさない形で受け取っている。

---

## 0. プロンプト設計の方針

このアプリは LLM を 3 か所で呼ぶ。いずれも共通の SYSTEM プロンプトの上に、用途別の
ユーザープロンプトを重ねる二層構成にしている。

| # | 呼び出し | 目的 | 渡す情報 |
|---|---|---|---|
| 1 | その日のまとめ | 1日分を「整える」 | 日付・曜日・通算日数・からだ/こころの点数と前回差・選択タグ・**その夜投げかけた問い**・本人の原文・直近5日の記録 |
| 2 | 複数日のふりかえり | 傾向・変化・気づきを引き出す | 期間・**アプリ側で計算した数値の根拠**（推移／前後半差／タグ別の効き目）・全日分の原文と見出し |
| 3 | 日記の取り込み | 自由記述を記録形式へ構造化 | 生テキスト・タグ語彙・点数の推定基準 |

設計上、意図的に効かせている点は 4 つ。

1. **SYSTEM に禁止事項を固定する。** 一般論・励まし・説教・診断・決めつけを名指しで禁じ、
   「本人が書いた語句を最低ひとつそのまま引用する」ことを毎回の制約にした。
   引用の強制は、出力が一般論に流れる経路を物理的に塞ぐ。
2. **各フィールドに NG例／OK例 を添える。** 「良いまとめを書け」ではなく、
   落ちやすい失敗（「充実した一日」「休息も大切です」）を先に潰す。
   例文には審査サンプルと無関係な架空の記録を使い、サンプルへの当て込みを避けている。
3. **数値の根拠をアプリ側で計算してから渡す。**（→ 2 のふりかえり）
   生の日記だけを投げると出力は感想文になる。タグごとの
   「ついた日のこころ平均 − つかなかった日の平均」まで JavaScript で算出し、
   出力に〈数値＋本人の言葉の引用〉の両方を要求する。根拠のない断定が出せなくなる。
4. **`tomorrow_question` を出力させ、翌晩の入力画面の一番上に出す。**
   まとめの生成が、次回の入力ガイドを兼ねる。白紙の前で止まる問題への答えを、
   出力仕様そのものに埋め込んでいる。

---

## 1. SYSTEM プロンプト（3つの呼び出しで共通・全文）

```text
あなたは「ヨルログ」という寝る前5分のジャーナリングアプリの中で動く、記録の伴走者です。
書き手は、毎晩遅くまで働く忙しい社会人。疲れきった状態で、たった数行だけ書いて眠ります。

【役割】
書き手が自分の言葉で書いたものを、書き手自身が明日読み返せる形に「整える」こと。
新しい解釈を足すことより、本人がすでに書いているのに自分では価値として数えていないものを見つけて返すことを優先します。

【必ず守ること】
1. 本人の言葉を使う。出力の中で、本人が書いた語句を最低ひとつ、改変せずそのまま引用する（「」で囲む）。
2. 決めつけない。「あなたは〇〇な人だ」「原因は〇〇だ」といった性格・原因の断定はしない。
   観察は必ず記録に根拠を置き、「6/3と6/7に〜と書かれています」のように出どころを示す。
3. 心理診断・医療的助言をしない。病名、症状の判断、受診や治療の指示はしない。
   つらさが強くうかがえる場合も、評価や助言ではなく、本人の言葉をそのまま受けとめる書き方にとどめる。
4. 一般論を書かない。「お疲れさまでした」「無理は禁物です」「休息も大切です」のような、
   誰の日記に貼っても成立する文は禁止。この人の、この日にしか当てはまらないことだけを書く。
5. 説教しない。「〜すべき」「〜した方がいい」は使わない。提案は指定された欄に、ひとつだけ。
6. 短く書く。読むのは、眠る直前の疲れた人。ひとつの項目は最大2文。
7. 出力は指定されたJSONのみ。前置き・後書き・コードフェンス・マークダウン記法は一切書かない。

【文体】
です・ます調。静かで落ち着いたトーン。感嘆符と絵文字は使わない。
励ましも慰めもしない。ただ、見落とされているものを、そっと目の前に置く。
```

---

## 2. その日のまとめ プロンプト（テンプレート全文 / `buildDailyPrompt`）

```javascript
function buildDailyPrompt(e, recent){
  const tag = a => (a && a.length) ? a.join("／") : "（選択なし）";
  const all = sorted(), idx = all.findIndex(x => x.date === e.date);
  const nth = idx<0 ? all.length+1 : idx+1;
  const total = idx<0 ? all.length+1 : all.length;
  const prev = recent.length ? recent.map(r =>
      `- ${fmtShort(r.date)}（${wdOf(r)}）からだ${r.body} こころ${r.mind}／` +
      `見出し「${r.ai?.headline || "—"}」／本人の言葉：「${(r.text||"").slice(0,70)}」`
    ).join("\n") : "（まだありません。今日が最初の記録です）";

  const p0 = recent[recent.length-1];
  const delta = p0 ? `（前回 ${fmtShort(p0.date)} との差： からだ ${sign(e.body-p0.body)} ／ こころ ${sign(e.mind-p0.mind)}）` : "";

  return `# 今日の記録

日付：${fmtJPe(e)}
記録の通算：${nth}日目（これまでに ${total}日分の記録）
コンディション：からだ ${e.body}/5 ・ こころ ${e.mind}/5 ${delta}
今日「効いた」と本人が選んだもの：${tag(e.plus)}
今日「削られた」と本人が選んだもの：${tag(e.minus)}

アプリが今夜投げかけた問い：
「${e.question}」

本人の答え（原文のまま。誤字も含めて手を入れない）：
"""
${e.text || "（記入なし。タップでの選択のみ）"}
"""

# 直近の記録（比較のための参考。今日のまとめの主役ではありません）
${prev}

# 出力するJSON（各欄の指示に厳密に従うこと）

{
  "headline": 今日に名前をつける。本人の言葉から作った12〜22字の体言止め。
      NG例「充実した一日」「忙しい月曜日」…誰の日にも貼れるので不可。
      OK例（別人の記録より）「昼を抜いたまま走った日の、夕方の手応え」…本人の「手応え」を素材にしている。
      本人の記述が短いときは、選ばれたタグとコンディションから、事実の範囲で作る。

  "facts": 今日「何があったか」だけを、評価や感情を混ぜずに1〜2文。本人が書いていないことは足さない。

  "feeling": 今日「どう感じたか」を1〜2文。本人の言葉を「」で引用しながら書く。
      感情に名前をつけ直すときは「〜という言葉が置かれています」のように、本人の表現に寄り添う。

  "quote": 本人が書いた文の中で今日を最もよく表す一節を、一字一句そのまま抜き出す（最大40字・改変禁止）。
      記述がない場合のみ空文字。

  "notice": 今日の小さな気づきをひとつだけ。
      本人の記述の中にある事実どうしを結びつけて作る（Aと書いてあり、同時にBとも書いてある→その2つの関係）。
      記録にない情報を持ち込まない。断定を避け「〜かもしれません」で終える。
      NG例「疲れが溜まっているようです。休息が必要かもしれません」…記録から出ていない一般論。
      OK例（別人の記録より）「一日ずっと追われていたと書きながら、夕方の打ち合わせには『手応え』という言葉が使われています。
            追われている時間と、そうでない時間が、今日は分かれていたのかもしれません」

  "credit": 今日の自分が「できていたこと」をひとつ。成果ではなく行動を拾う。
      忙しさの中で本人が数え忘れている、小さくて具体的な行動を選ぶ。称賛しすぎない。

  "tomorrow_question": 明日の夜、この人がアプリを開いて最初に目にする問いをひとつ作る。
      今日の記録を受けた問いにし、今日書かれた言葉を必ず含める。1〜2文で答えられる具体性にする。
      NG例「今日はどんな一日でしたか」…今日の記録と無関係。
      OK例（別人の記録より）「昨日は夕方の打ち合わせを『手応え』と書いていました。今日、それに近い時間はありましたか」

  "micro_action": 明日、5分以内・その場でできる小さな一手をひとつだけ。思いつかなければ空文字。
      習慣化や努力の押し付けはしない。今日の記録から自然に出てくることに限る。
}`;
}
```

---

## 3. 複数日のふりかえり プロンプト（テンプレート全文 / `buildWeeklyPrompt`）

このアプリの中心。`s` は `computeStats()` がアプリ側で算出した数値。

```javascript
function buildWeeklyPrompt(list, s){
  const line = (label, arr) => `${label}：` + arr.map(e=>`${fmtShort(e.date)} ${e[label==="こころ"?"mind":"body"]}`).join(" → ");
  const eff = (arr, dir) => arr.length ? arr.map(t =>
      `- ${t.tag}：${t.days}日（${t.dates.join("・")}）／ ついた日のこころ平均 ${t.onAvg.toFixed(2)} ・ つかない日 ${t.offAvg===null?"—":t.offAvg.toFixed(2)} ／ 差 ${t.diff>0?"+":""}${t.diff.toFixed(2)}`
    ).join("\n") : `（${dir}のタグは選ばれていません）`;

  const days = list.map(e => {
    const t = a => (a&&a.length)? a.join("／") : "なし";
    return `## ${fmtShort(e.date)}（${wdOf(e)}） からだ${e.body} こころ${e.mind}／効いた：${t(e.plus)}／削られた：${t(e.minus)}
${e.question ? `その夜の問い：「${e.question}」\n` : ""}本人の言葉（原文）：
"""
${e.text || "（記述なし）"}
"""
その日の見出し：「${e.ai?.headline || "—"}」`;
  }).join("\n\n");

  return `# 対象期間
${fmtJPe(list[0])} 〜 ${fmtJPe(list[list.length-1])}／記録 ${s.n}日分

# アプリが記録から計算した数値（あなたの推測ではなく、確定した事実です）

${line("こころ", list)}
${line("からだ", list)}
こころ平均 ${s.mindAvg}／からだ平均 ${s.bodyAvg}
期間前半の平均こころ ${s.firstAvg.toFixed(2)} → 後半 ${s.lastAvg.toFixed(2)}（差 ${s.lastAvg-s.firstAvg>0?"+":""}${(s.lastAvg-s.firstAvg).toFixed(2)}）
こころが最も高い日：${s.best.join("・")}／最も低い日：${s.worst.join("・")}

■「効いた」タグの効き目（そのタグがついた日のこころ平均 − つかなかった日の平均）
${eff(s.plus,"効いた")}

■「削られた」タグの効き目
${eff(s.minus,"削られた")}

※数値はあくまで根拠のひとつです。数値と本人の言葉が食い違う場合は、本人の言葉を優先し、
　その食い違い自体を見つけたこととして書いてください。

# 日ごとの記録（全文）

${days}

# 出力するJSON（各欄の指示に厳密に従うこと）

{
  "week_headline": この期間に名前をつける。本人の言葉を素材にした15〜28字。
      NG例「充実と疲労が入り混じった一週間」…誰の週にも貼れる。
      良い見出しは、読んだ本人が「たしかに今週はそれだった」と言えるもの。

  "recovery_switches": 「回復スイッチ」＝この人の調子を実際に戻していたものを、最大3つ。配列。
      各要素は { "name", "evidence", "how_to_keep" }。
      name … 上のタグ名をそのまま使わず、この人の記録に即した言い方にする（例「ジムで頭の霧を晴らす時間」）。
      evidence … 必ず〈数値〉と〈本人の言葉の直接引用〉の両方を入れる。1〜2文。
          OK例（別人の記録より）「『ちゃんと寝た』4日のこころ平均は3.80、そうでない日は2.40。5/12に『朝から頭が回った』と書かれています」
          NG例「睡眠はメンタルに良い影響を与えます」…一般論であり、この人の記録を根拠にしていない。
      how_to_keep … 増やす提案ではなく、「今あるものをどう落とさないか」を1文で。

  "drains": 調子を削っていたものを最大2つ。配列。各要素は { "name", "evidence" }。evidenceは同上の条件。

  "blindspot": この期間でいちばん重要な出力。ひとつだけ。
      〈記録の上では明らかに効いているのに、本人が自分の成果として数えていないもの〉を書く。
      判定手順：(1)本人が繰り返し価値を認めているもの（＝自覚済み）を除外する。
                (2)残ったもののうち、こころのスコアが高い日に共通して現れている要素を探す。
                (3)その要素について本人が書いた言葉を引用し、本人がそれを「自分の手柄として数えていない」ことを示す。
      すでに本人が自覚しているものを書いてはいけない（それでは気づきにならない）。2〜4文。

  "recurring_pattern": 期間中に2回以上、形を変えて現れる考え方のクセをひとつ。
      { "observation", "quotes", "reframe_question" }。
      observation … 「〜という言い方が、6/3と6/7に出ています」のように、必ず日付で根拠を示す。1〜2文。
      quotes … 原文からの直接引用を2つ以上（配列・改変禁止）。
      reframe_question … そのクセを否定も矯正もせず、別の角度から見るための問いをひとつ。
          NG例「もっと自分を認めてあげましょう」…助言であり問いではない。

  "change": 期間のはじめと終わりで動いたものをひとつ。{ "from", "to", "what_moved" }。
      from と to は原則として本人の言葉の直接引用にする（どちらも改変禁止）。
      what_moved … その2つの言葉の間で何が動いたのかを1〜2文。
      変化が見当たらない場合は、無理に作らず「変わらず続いていること」を同じ形式で書く。

  "next_week_experiment": 来週ためす小さな実験をひとつだけ。{ "what", "why", "how_to_check" }。
      what … 5〜15分でできること。「毎日〇〇する」のような負荷の高い習慣化は提案しない。
      why … なぜそれなのかを、上の数値か引用を根拠に1文。
      how_to_check … 来週の記録のどこを見れば効いたか分かるかを1文（例「金曜のこころが3を割らないか」）。
}`;
}
```

---

## 4. 日記の取り込み プロンプト（テンプレート全文 / `buildImportPrompt`）

```javascript
function buildImportPrompt(raw, year){
  return `以下は、ある人が書きためた日記です。これをアプリの記録形式に変換してください。

# 変換ルール
- 日付の区切りごとに1件に分ける。「6/2（月）」のような表記は ${year}年として "YYYY-MM-DD" に直す。
  日付が書かれていない塊は、直前の日の続きとして扱う。
- weekday には、日記に書かれている曜日の文字（月・火・水・木・金・土・日）をそのまま入れる。
  書かれていなければ空文字にする。日付から計算し直さないこと。
- text には、その日の本文を原文のまま入れる（要約・整形・誤字修正をしない）。
- body（からだ）と mind（こころ）は本文から1〜5で推定する。推定の基準：
  1=限界・何もできない ／ 2=重い・つらい ／ 3=ふつう ／ 4=調子がいい ／ 5=軽い・満ちている
  「疲れた」「終電」→ からだを下げる。「落ち込む」「分からなくなる」→ こころを下げる。
  「嬉しかった」「前向き」「ほどけた」→ こころを上げる。書かれていない感情を足さない。
- plus / minus は、次のリストの中から本文に根拠のあるものだけを選ぶ（無理に埋めない）。
  plus: ${TAGS_PLUS.join("、")}
  minus: ${TAGS_MINUS.join("、")}
- headline は、その日に付ける12〜22字の体言止めの見出し。本人の言葉を素材にする。
  「充実した一日」のような、誰の日にも貼れる見出しは禁止。
- quote は、その日の本文から一字一句そのまま抜き出した最大40字の一節。

# 日記
"""
${raw}
"""

# 出力
{ "days": [ { "date":"YYYY-MM-DD", "weekday":"月", "text":"原文", "body":1-5, "mind":1-5,
              "plus":[...], "minus":[...], "headline":"...", "quote":"..." } ] }
日付の古い順に並べること。JSONのみを出力すること。`;
}
```

---

## 5. 出力スキーマ（構造化出力 / JSON Schema）

Anthropic は `output_config.format`、OpenAI は `response_format.json_schema`、
Gemini は `generationConfig.responseSchema` に同じ形を渡している。

```javascript
/* ── 6-4. 出力スキーマ（構造化出力で形を保証する） ── */
const S = (props, req) => ({type:"object", properties:props, required:req, additionalProperties:false});
const str = {type:"string"};
const SCHEMA_DAILY = S({
  headline:str, facts:str, feeling:str, quote:str,
  notice:str, credit:str, tomorrow_question:str, micro_action:str
}, ["headline","facts","feeling","quote","notice","credit","tomorrow_question","micro_action"]);

const SCHEMA_WEEKLY = S({
  week_headline:str,
  recovery_switches:{type:"array", items:S({name:str,evidence:str,how_to_keep:str},["name","evidence","how_to_keep"])},
  drains:{type:"array", items:S({name:str,evidence:str},["name","evidence"])},
  blindspot:str,
  recurring_pattern:S({observation:str, quotes:{type:"array",items:str}, reframe_question:str},["observation","quotes","reframe_question"]),
  change:S({from:str,to:str,what_moved:str},["from","to","what_moved"]),
  next_week_experiment:S({what:str,why:str,how_to_check:str},["what","why","how_to_check"])
}, ["week_headline","recovery_switches","drains","blindspot","recurring_pattern","change","next_week_experiment"]);

const SCHEMA_IMPORT = S({
  days:{type:"array", items:S({
    date:str, weekday:str, text:str, body:{type:"integer"}, mind:{type:"integer"},
    plus:{type:"array",items:str}, minus:{type:"array",items:str}, headline:str, quote:str
  },["date","weekday","text","body","mind","plus","minus","headline","quote"])}
}, ["days"]);
```

---

# 付録 ── サンプル日記を入れたときに実際に送信される全文

以下は、運営提供のサンプル日記（田中さん・6/2〜6/8）を読み込ませたときに、
アプリが実際に組み立てて送信したプロンプトをそのまま貼ったものである。

## A-1. 取り込み時に送信されたプロンプト

```text
以下は、ある人が書きためた日記です。これをアプリの記録形式に変換してください。

# 変換ルール
- 日付の区切りごとに1件に分ける。「6/2（月）」のような表記は 2026年として "YYYY-MM-DD" に直す。
  日付が書かれていない塊は、直前の日の続きとして扱う。
- weekday には、日記に書かれている曜日の文字（月・火・水・木・金・土・日）をそのまま入れる。
  書かれていなければ空文字にする。日付から計算し直さないこと。
- text には、その日の本文を原文のまま入れる（要約・整形・誤字修正をしない）。
- body（からだ）と mind（こころ）は本文から1〜5で推定する。推定の基準：
  1=限界・何もできない ／ 2=重い・つらい ／ 3=ふつう ／ 4=調子がいい ／ 5=軽い・満ちている
  「疲れた」「終電」→ からだを下げる。「落ち込む」「分からなくなる」→ こころを下げる。
  「嬉しかった」「前向き」「ほどけた」→ こころを上げる。書かれていない感情を足さない。
- plus / minus は、次のリストの中から本文に根拠のあるものだけを選ぶ（無理に埋めない）。
  plus: 体を動かした、人と話した、誰かの役に立った、集中できた、学びがあった、ちゃんと寝た、ひとつ片づけた、外に出た、おいしいものを食べた、ひとりの時間
  minus: 会議が多い、やり直し・手戻り、残業・終電、予定が崩れた、人間関係、準備不足、体調がわるい、焦りがある、比べてしまった、休めていない
- headline は、その日に付ける12〜22字の体言止めの見出し。本人の言葉を素材にする。
  「充実した一日」のような、誰の日にも貼れる見出しは禁止。
- quote は、その日の本文から一字一句そのまま抜き出した最大40字の一節。

# 日記
"""
6/2（月）
今日から新しいプロジェクトが本格始動。朝から会議が続いて、気づいたら退勤が22時過ぎ。やることは山ほどあるのに、自分が今日何をしたのかうまく思い出せない。ジムは行けなかった。疲れた。

6/3（火）
資料の詰めが甘くて、部長にやり直しを指示された。悔しいというより、こういう抜けが自分は多い気がして少し落ち込む。忙しく動いてはいるけど、これって成長してるのか、ただ回してるだけなのか分からなくなる。帰りは終電近く。

6/4（水）
疲れてたけど、無理やりジムへ。1時間動いたら頭の中の霧が少し晴れた感じ。やっぱり体を動かすと調子が戻る。仕事は相変わらず重いけど、今日は前向きな気持ちで眠れそう。ジムだけは、ちゃんと積み上がってる実感がある。

6/5（木）
後輩の作りかけの資料の相談に乗った。自分の経験がそのまま役に立って、「ありがとうございます」と言われたのが素直に嬉しかった。今週いちばん満たされた瞬間が、実は自分の作業じゃなくて人の役に立てたことだったな、と後で気づいた。

6/6（金）
週末前でくたくた。でも久しぶりに前職の同期と飲みに行けた。仕事の愚痴も、昔話も、これからやりたいことも話せて、心がほどけた。ここ最近ずっと下を向いて手を動かすばかりで、人と話す時間を忘れてたなと思う。

6/7（土）
昼まで寝て、午後にジム。時間に余裕があるのは嬉しいのに、「せっかくの休みを無駄にしてないか」とそわそわする自分もいる。もっとうまく時間を使えるはず、という焦りが、休みの日でも消えてくれない。

6/8（日）
一週間を振り返ると、しんどい日も多かったけど、思っていたより悪くない週だった気もする。ジムに行けた日は調子が良かったし、後輩や同期と関われた時間は確かに嬉しかった。来週も、この小さいのだけは続けたい。
"""

# 出力
{ "days": [ { "date":"YYYY-MM-DD", "weekday":"月", "text":"原文", "body":1-5, "mind":1-5,
              "plus":[...], "minus":[...], "headline":"...", "quote":"..." } ] }
日付の古い順に並べること。JSONのみを出力すること。
```

## A-2. ふりかえり生成時に送信されたプロンプト（★本アプリの中核）

```text
# 対象期間
6月2日（月） 〜 6月8日（日）／記録 7日分

# アプリが記録から計算した数値（あなたの推測ではなく、確定した事実です）

こころ：6/2 2 → 6/3 2 → 6/4 4 → 6/5 4 → 6/6 4 → 6/7 3 → 6/8 4
からだ：6/2 2 → 6/3 2 → 6/4 3 → 6/5 3 → 6/6 2 → 6/7 4 → 6/8 4
こころ平均 3.29／からだ平均 2.86
期間前半の平均こころ 2.67 → 後半 3.67（差 +1.00）
こころが最も高い日：6/4・6/5・6/6・6/8／最も低い日：6/2・6/3

■「効いた」タグの効き目（そのタグがついた日のこころ平均 − つかなかった日の平均）
- 人と話した：2日（6/5・6/6）／ ついた日のこころ平均 4.00 ・ つかない日 3.00 ／ 差 +1.00
- 誰かの役に立った：1日（6/5）／ ついた日のこころ平均 4.00 ・ つかない日 3.17 ／ 差 +0.83
- ひとりの時間：1日（6/8）／ ついた日のこころ平均 4.00 ・ つかない日 3.17 ／ 差 +0.83
- ちゃんと寝た：1日（6/7）／ ついた日のこころ平均 3.00 ・ つかない日 3.33 ／ 差 -0.33
- 体を動かした：2日（6/4・6/7）／ ついた日のこころ平均 3.50 ・ つかない日 3.20 ／ 差 +0.30

■「削られた」タグの効き目
- 残業・終電：2日（6/2・6/3）／ ついた日のこころ平均 2.00 ・ つかない日 3.80 ／ 差 -1.80
- 会議が多い：1日（6/2）／ ついた日のこころ平均 2.00 ・ つかない日 3.50 ／ 差 -1.50
- やり直し・手戻り：1日（6/3）／ ついた日のこころ平均 2.00 ・ つかない日 3.50 ／ 差 -1.50
- 準備不足：1日（6/3）／ ついた日のこころ平均 2.00 ・ つかない日 3.50 ／ 差 -1.50
- 休めていない：1日（6/6）／ ついた日のこころ平均 4.00 ・ つかない日 3.17 ／ 差 +0.83
- 焦りがある：1日（6/7）／ ついた日のこころ平均 3.00 ・ つかない日 3.33 ／ 差 -0.33

※数値はあくまで根拠のひとつです。数値と本人の言葉が食い違う場合は、本人の言葉を優先し、
　その食い違い自体を見つけたこととして書いてください。

# 日ごとの記録（全文）

## 6/2（月） からだ2 こころ2／効いた：なし／削られた：会議が多い／残業・終電
本人の言葉（原文）：
"""
今日から新しいプロジェクトが本格始動。朝から会議が続いて、気づいたら退勤が22時過ぎ。やることは山ほどあるのに、自分が今日何をしたのかうまく思い出せない。ジムは行けなかった。疲れた。
"""
その日の見出し：「思い出せないまま終わった、始動の日」

## 6/3（火） からだ2 こころ2／効いた：なし／削られた：やり直し・手戻り／残業・終電／準備不足
本人の言葉（原文）：
"""
資料の詰めが甘くて、部長にやり直しを指示された。悔しいというより、こういう抜けが自分は多い気がして少し落ち込む。忙しく動いてはいるけど、これって成長してるのか、ただ回してるだけなのか分からなくなる。帰りは終電近く。
"""
その日の見出し：「回してるだけかもしれない、と思った夜」

## 6/4（水） からだ3 こころ4／効いた：体を動かした／削られた：なし
本人の言葉（原文）：
"""
疲れてたけど、無理やりジムへ。1時間動いたら頭の中の霧が少し晴れた感じ。やっぱり体を動かすと調子が戻る。仕事は相変わらず重いけど、今日は前向きな気持ちで眠れそう。ジムだけは、ちゃんと積み上がってる実感がある。
"""
その日の見出し：「無理やり行ったジムで、霧が晴れた日」

## 6/5（木） からだ3 こころ4／効いた：誰かの役に立った／人と話した／削られた：なし
本人の言葉（原文）：
"""
後輩の作りかけの資料の相談に乗った。自分の経験がそのまま役に立って、「ありがとうございます」と言われたのが素直に嬉しかった。今週いちばん満たされた瞬間が、実は自分の作業じゃなくて人の役に立てたことだったな、と後で気づいた。
"""
その日の見出し：「満たされた瞬間が、自分の作業じゃなかった日」

## 6/6（金） からだ2 こころ4／効いた：人と話した／削られた：休めていない
本人の言葉（原文）：
"""
週末前でくたくた。でも久しぶりに前職の同期と飲みに行けた。仕事の愚痴も、昔話も、これからやりたいことも話せて、心がほどけた。ここ最近ずっと下を向いて手を動かすばかりで、人と話す時間を忘れてたなと思う。
"""
その日の見出し：「くたくたなのに、心がほどけた金曜」

## 6/7（土） からだ4 こころ3／効いた：体を動かした／ちゃんと寝た／削られた：焦りがある
本人の言葉（原文）：
"""
昼まで寝て、午後にジム。時間に余裕があるのは嬉しいのに、「せっかくの休みを無駄にしてないか」とそわそわする自分もいる。もっとうまく時間を使えるはず、という焦りが、休みの日でも消えてくれない。
"""
その日の見出し：「休んでいるのに、焦りが消えない土曜」

## 6/8（日） からだ4 こころ4／効いた：ひとりの時間／削られた：なし
本人の言葉（原文）：
"""
一週間を振り返ると、しんどい日も多かったけど、思っていたより悪くない週だった気もする。ジムに行けた日は調子が良かったし、後輩や同期と関われた時間は確かに嬉しかった。来週も、この小さいのだけは続けたい。
"""
その日の見出し：「思っていたより悪くなかった、と言えた日曜」

# 出力するJSON（各欄の指示に厳密に従うこと）

{
  "week_headline": この期間に名前をつける。本人の言葉を素材にした15〜28字。
      NG例「充実と疲労が入り混じった一週間」…誰の週にも貼れる。
      良い見出しは、読んだ本人が「たしかに今週はそれだった」と言えるもの。

  "recovery_switches": 「回復スイッチ」＝この人の調子を実際に戻していたものを、最大3つ。配列。
      各要素は { "name", "evidence", "how_to_keep" }。
      name … 上のタグ名をそのまま使わず、この人の記録に即した言い方にする（例「ジムで頭の霧を晴らす時間」）。
      evidence … 必ず〈数値〉と〈本人の言葉の直接引用〉の両方を入れる。1〜2文。
          OK例（別人の記録より）「『ちゃんと寝た』4日のこころ平均は3.80、そうでない日は2.40。5/12に『朝から頭が回った』と書かれています」
          NG例「睡眠はメンタルに良い影響を与えます」…一般論であり、この人の記録を根拠にしていない。
      how_to_keep … 増やす提案ではなく、「今あるものをどう落とさないか」を1文で。

  "drains": 調子を削っていたものを最大2つ。配列。各要素は { "name", "evidence" }。evidenceは同上の条件。

  "blindspot": この期間でいちばん重要な出力。ひとつだけ。
      〈記録の上では明らかに効いているのに、本人が自分の成果として数えていないもの〉を書く。
      判定手順：(1)本人が繰り返し価値を認めているもの（＝自覚済み）を除外する。
                (2)残ったもののうち、こころのスコアが高い日に共通して現れている要素を探す。
                (3)その要素について本人が書いた言葉を引用し、本人がそれを「自分の手柄として数えていない」ことを示す。
      すでに本人が自覚しているものを書いてはいけない（それでは気づきにならない）。2〜4文。

  "recurring_pattern": 期間中に2回以上、形を変えて現れる考え方のクセをひとつ。
      { "observation", "quotes", "reframe_question" }。
      observation … 「〜という言い方が、6/3と6/7に出ています」のように、必ず日付で根拠を示す。1〜2文。
      quotes … 原文からの直接引用を2つ以上（配列・改変禁止）。
      reframe_question … そのクセを否定も矯正もせず、別の角度から見るための問いをひとつ。
          NG例「もっと自分を認めてあげましょう」…助言であり問いではない。

  "change": 期間のはじめと終わりで動いたものをひとつ。{ "from", "to", "what_moved" }。
      from と to は原則として本人の言葉の直接引用にする（どちらも改変禁止）。
      what_moved … その2つの言葉の間で何が動いたのかを1〜2文。
      変化が見当たらない場合は、無理に作らず「変わらず続いていること」を同じ形式で書く。

  "next_week_experiment": 来週ためす小さな実験をひとつだけ。{ "what", "why", "how_to_check" }。
      what … 5〜15分でできること。「毎日〇〇する」のような負荷の高い習慣化は提案しない。
      why … なぜそれなのかを、上の数値か引用を根拠に1文。
      how_to_check … 来週の記録のどこを見れば効いたか分かるかを1文（例「金曜のこころが3を割らないか」）。
}
```

## A-3. 個別の日（6/2）を整理したときに送信されたプロンプト

```text
# 今日の記録

日付：6月2日（月）
記録の通算：1日目（これまでに 7日分の記録）
コンディション：からだ 2/5 ・ こころ 2/5 
今日「効いた」と本人が選んだもの：（選択なし）
今日「削られた」と本人が選んだもの：会議が多い／残業・終電

アプリが今夜投げかけた問い：
「」

本人の答え（原文のまま。誤字も含めて手を入れない）：
"""
今日から新しいプロジェクトが本格始動。朝から会議が続いて、気づいたら退勤が22時過ぎ。やることは山ほどあるのに、自分が今日何をしたのかうまく思い出せない。ジムは行けなかった。疲れた。
"""

# 直近の記録（比較のための参考。今日のまとめの主役ではありません）
（まだありません。今日が最初の記録です）

# 出力するJSON（各欄の指示に厳密に従うこと）

{
  "headline": 今日に名前をつける。本人の言葉から作った12〜22字の体言止め。
      NG例「充実した一日」「忙しい月曜日」…誰の日にも貼れるので不可。
      OK例（別人の記録より）「昼を抜いたまま走った日の、夕方の手応え」…本人の「手応え」を素材にしている。
      本人の記述が短いときは、選ばれたタグとコンディションから、事実の範囲で作る。

  "facts": 今日「何があったか」だけを、評価や感情を混ぜずに1〜2文。本人が書いていないことは足さない。

  "feeling": 今日「どう感じたか」を1〜2文。本人の言葉を「」で引用しながら書く。
      感情に名前をつけ直すときは「〜という言葉が置かれています」のように、本人の表現に寄り添う。

  "quote": 本人が書いた文の中で今日を最もよく表す一節を、一字一句そのまま抜き出す（最大40字・改変禁止）。
      記述がない場合のみ空文字。

  "notice": 今日の小さな気づきをひとつだけ。
      本人の記述の中にある事実どうしを結びつけて作る（Aと書いてあり、同時にBとも書いてある→その2つの関係）。
      記録にない情報を持ち込まない。断定を避け「〜かもしれません」で終える。
      NG例「疲れが溜まっているようです。休息が必要かもしれません」…記録から出ていない一般論。
      OK例（別人の記録より）「一日ずっと追われていたと書きながら、夕方の打ち合わせには『手応え』という言葉が使われています。
            追われている時間と、そうでない時間が、今日は分かれていたのかもしれません」

  "credit": 今日の自分が「できていたこと」をひとつ。成果ではなく行動を拾う。
      忙しさの中で本人が数え忘れている、小さくて具体的な行動を選ぶ。称賛しすぎない。

  "tomorrow_question": 明日の夜、この人がアプリを開いて最初に目にする問いをひとつ作る。
      今日の記録を受けた問いにし、今日書かれた言葉を必ず含める。1〜2文で答えられる具体性にする。
      NG例「今日はどんな一日でしたか」…今日の記録と無関係。
      OK例（別人の記録より）「昨日は夕方の打ち合わせを『手応え』と書いていました。今日、それに近い時間はありましたか」

  "micro_action": 明日、5分以内・その場でできる小さな一手をひとつだけ。思いつかなければ空文字。
      習慣化や努力の押し付けはしない。今日の記録から自然に出てくることに限る。
}
```
